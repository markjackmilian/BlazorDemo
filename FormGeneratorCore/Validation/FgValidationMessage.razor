@namespace FormGenerator.Core.Validation

@typeparam TValue
@inherits FgValidationMessageComponent<TValue>

@foreach (var message in ValidationMessages)
{
    <div class="@Class">
        @message
    </div>
}
